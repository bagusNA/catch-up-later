# Backend origin used by the Nuxt dev proxy for /api/**.
NUXT_API_PROXY_TARGET=http://localhost:8080

# Base path used by the frontend API client. Keep same-origin in development
# and behind a reverse proxy in production.
NUXT_PUBLIC_API_BASE=/api/v1
