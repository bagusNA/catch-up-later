package com.bagusna.catchuplater.common.api

/**
 * Canonical API path constants.
 *
 * Every public JSON endpoint lives under the single versioned base [V1].
 */
object ApiRoutes {
    const val V1: String = "/api/v1"
}
